# 提交与验收规范

每次培训的具体要求以对应的 `任务.md` 为准。这份文件只说明提交方式和验收方式。

整个培训阶段建议使用同一个个人 GitHub 仓库，连续保存各阶段代码和记录。

## 每次任务提交什么

个人仓库可以按培训顺序组织：

```text
quadruped-training-xxx/
├── README.md
├── 01_linux/
├── 02_mujoco/
├── 03_motor/
└── ...
```

目录名可以自行调整，只要能看出各部分对应的任务。

一次任务至少包含：

- 源代码、配置和脚本；
- 简短 README；
- 必要的运行结果；
- 对应的 Git 提交记录。

README 至少说明：

```text
这个目录完成了什么
需要什么环境
怎么运行
主要文件分别做什么
```

终端输出、截图或简短视频都可以作为运行结果。

构建产物、缓存、无关大型文件、密码、Token、账号信息等不要提交。逐步使用 `.gitignore` 管理这些文件。

第三方模型、数据等体积较大的资源，如果可以重新下载，可以在 README 中写明来源和获取方式。

## Git 提交

正式任务使用 Git 管理并同步到 GitHub。

至少需要熟悉：

```text
git clone
git status
git diff
git add
git commit
git pull
git push
```

开发过程中按实际进度提交，例如：

```text
建立基础目录
完成模型加载
加入场景
修正初始姿态
补充 README
```

commit message 能说明当前修改内容即可。

提交前检查：

```bash
git status
git diff
git diff --staged
```

用于验收的最新版本应已经推送到 GitHub。

公开仓库可以直接访问。使用 Private 仓库时，需要给负责培训和验收的人访问权限。

## 当面验收

GitHub 主要用于留档和提前查看。正式验收时会现场运行代码，并围绕本人提交的内容提问。

主要看四项：

1. 是否完成任务要求；
2. 程序是否能正常运行；
3. 是否能说明主要结构、变量、接口和配置；
4. 是否能完成一个与当前任务相关的小修改或简单排错。

现场问题以当前任务内容为主，不考无关的冷门知识。

如果程序能运行，但本人无法说明核心代码的作用，仍需要继续补充学习。

## AI 和外部代码

培训允许使用 AI，也允许参考开源项目和其他资料。

AI 可以用于：

- 解释概念；
- 分析报错；
- 查询 API 和命令；
- 检查自己写的代码；
- 帮助定位问题；
- 整理 README。

进入自己仓库的代码应当能够说明用途，并且能够继续修改。直接生成完整工程后提交，但对主要结构和代码不了解，不满足验收要求。

引用明显来自外部项目的代码、模型或资料时，在 README 中保留基本来源。

## 验收结果

### 通过

主要任务已经完成，程序可以运行，能够解释提交内容并完成合理的小修改。

### 修改后通过

主体已经完成，但还存在少量问题，例如：

- README 缺失；
- 目录较乱；
- 小功能错误；
- 个别部分解释不清。

按要求修改后即可通过，一般不需要重新做完整验收。

### 补验

以下情况需要修改后重新验收：

- 主要任务没有完成；
- 程序无法运行；
- 提交内容明显不完整；
- 无法说明核心代码；
- 大量提交自己不了解的完整实现；
- 没有按要求完成 GitHub 提交。

## 基本工程习惯

培训初期不要求正式项目级的工程规范，但代码至少应当便于自己和别人继续修改。

建议做到：

- 文件和目录命名清楚；
- 功能增加后及时拆分过大的文件；
- 少用只在自己电脑上成立的绝对路径；
- 集中管理环境相关参数；
- README 中的运行方式与实际代码一致；
- 临时调试代码用完后及时整理。

课程、比赛、设备等原因导致延期时，提前说明情况。完成后按正常 Git 提交流程补交和补验即可。

## 总体要求

验收最终关注三件事：

**做出来、能运行、讲得清楚。**
