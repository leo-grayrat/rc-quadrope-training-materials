# 四足组培训任务提交与验收规范

这份规范只规定**怎样提交和验收**，不重新解释每次培训任务。每次任务具体要做什么，以对应培训目录中的人工任务单为准；配套讲义用于学习和排错，不覆盖任务单要求。

整个培训阶段尽量使用同一个个人 GitHub 仓库，保留从基础环境、仿真到后续机器人控制的完整过程。

---

## 1. 每次任务提交什么

个人仓库可以按培训顺序组织，例如：

```text
quadruped-training-xxx/
├── README.md
├── 01_linux/
├── 02_mujoco/
├── 03_motor/
└── ...
```

具体目录名可以自己调整，但应让别人能看出每部分对应什么任务。

一次正式任务至少应包含：

- 完成任务所需的源代码、配置和脚本；
- 一个简短 README，说明怎么运行、主要文件是什么；
- 必要的运行结果，例如终端输出、截图或简短视频；
- 能反映开发过程的正常 Git 提交记录。

README 不需要写成长报告。别人按照它至少应该能知道：

```text
这个目录做了什么
依赖什么环境
怎么运行
主要文件分别干什么
```

不要提交明显无关的构建产物、缓存、大型数据、密钥、Token、账号密码等内容。逐步使用 `.gitignore` 管理这些文件。

---

## 2. Git 提交要求

从正式任务开始，统一使用 Git 管理并同步到 GitHub。

至少应能自己使用：

```text
git clone
git status
git diff
git add
git commit
git pull
git push
```

任务做完以后不要只留下一个内容含糊的 `final` 提交。正常开发过程中可以按阶段提交，例如：

```text
建立基础目录
完成模型加载
加入场景
修正初始姿态
补充 README
```

Commit message 不要求严格格式，但应能大致说明这次修改做了什么。

提交前自己确认：

```bash
git status
git diff
git diff --staged
```

GitHub 上应能看到用于验收的最新版本。

---

## 3. 当面验收看什么

GitHub 用于留档和提前检查；正式验收主要看本人能不能把自己提交的东西跑起来、讲清楚、稍微改一下。

验收通常围绕四件事：

1. **任务要求有没有完成**：以对应任务单为准；
2. **程序能不能实际运行**：现场运行的代码应与提交内容一致；
3. **能不能解释关键部分**：不要求逐行背代码，但要知道主要结构和关键变量、接口、配置的作用；
4. **能不能做一个小修改或排错**：例如改参数、增加一个简单功能、解释一条报错来自哪里。

问题优先来自本人刚做过的任务，不用冷门知识卡人。

如果代码能运行，但本人完全不知道核心部分在做什么，不视为已经完成这一阶段的学习目标。

---

## 4. AI 可以怎么用

培训不禁止 AI。可以用它：

- 解释概念；
- 看报错；
- 查某个 API 或命令；
- 对自己写的代码提出修改建议；
- 帮助定位问题；
- 整理 README。

不建议把任务原文一次性交给 AI，让它生成一个完整工程后直接提交。这样即使程序暂时能跑，也很容易在现场修改、后续任务或者真实机器人代码里断掉。

一个简单判断标准是：

> AI 给出的内容进入自己的仓库以后，自己应该能解释它为什么存在、改动会影响什么，并能在需要时继续修改。

外部示例、开源代码和 AI 代码都一样，不要求“必须全部手写”，但不能把完全不理解的成品当作自己的学习结果。

---

## 5. 验收结果

### 通过

主要要求完成，能够正常运行，对自己提交的内容有基本理解，也能完成合理的小修改。

### 修改后通过

主体已经完成，但有少量问题，例如 README 缺失、目录混乱、小功能错误或部分解释不清。按要求修正后即可通过，一般不需要完整重验。

### 补验

出现下面这类情况，需要完成修改后重新验收：

- 主要任务没有完成；
- 程序无法运行；
- 提交内容明显不完整；
- 无法解释自己代码的基本逻辑；
- 大量直接复制完整实现且本人不了解；
- 没有按要求完成基本 GitHub 提交。

补验的目的不是处罚，而是避免基础没有建立就继续进入下一阶段。

---

## 6. 最后只看三件事

整个规范可以压缩成一句话：

**做出来、能运行、讲得清楚。**

GitHub 负责把过程和成果留下来，当面验收负责确认这些东西确实已经变成了自己的能力。
