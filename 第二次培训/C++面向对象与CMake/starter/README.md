# Starter 代码

每个目录对应正文中的一次动手。

- `01_motor/`：类、对象、成员变量与成员函数
- `02_encapsulation/`：封装、构造函数、`const`
- `03_polymorphism/`：抽象接口、继承与多态
- `04_robot/`：组合与 `Motor&`
- `05_cmake/`：多文件工程与 CMake

正文只说明任务和预期行为；真正需要填写的 TODO 都在这里。
